{
  "name": "iching-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "wterm-based web playback of the iching cast ritual (experiment).",
  "scripts": {
    "serve": "bun serve.ts"
  },
  "dependencies": {
    "@wterm/dom": "^0.3.0"
  }
}
